<% entry = LibraryEntry.find_by(user: @user, movie: @movie) %>
<%= button_to movie_toggle_watchlist_path(@movie), 
                method: :post, 
                class: "btn #{entry&.in_watchlist ? 'btn-success' : 'btn-outline-secondary'}" do %>
    <%= entry&.in_watchlist ? "✓ In Watchlist" : "+ Add to Watchlist" %>
  <% end %>
