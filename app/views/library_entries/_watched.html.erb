<% entry = LibraryEntry.find_by(user: @user, movie: @movie) %>
<% watched = entry&.watched_date.present? %>

<%= button_to movie_toggle_watched_path(@movie), 
                method: :post, 
                class: "btn #{entry&.watched ? 'btn-info' : 'btn-outline-primary'}" do %>
    <%= watched ? "★ Watched" : "Mark as Watched" %>
  <% end %>




